parameters:
  - name: artifact
    default: extension
  - name: env
    default: dev
    values:
      - dev
      - qa
      - prod

steps:
  - task: Npm@1
    displayName: 'Compile - ${{ parameters.env }}'
    inputs:
      command: custom
      customCommand: 'run build:${{ parameters.env }}'
